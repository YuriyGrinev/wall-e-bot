# Телеграм-бот для проекта WALL-E

Для тестирования используйте:
`python3 -m venv venv`
`source ./venv/bin/activate`
`pip install -r requirements.txt`
`python main.py`

Не забудьте создать свой `config/config.py` с токеном для бота
